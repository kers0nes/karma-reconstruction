{
  "name": "karma-reconstruction",
  "version": "1.0.0",
  "description": "Static site for Render deployment",
  "main": "index.html",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "build": "echo \"No build step needed\""
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
